---
interface Props {
    title: string;
    description: string;
    imageUrl: string;
    elements?: Array<{
        icon?: string;
        text: string;
    }>;
}

const { title, description, imageUrl, elements = [] } = Astro.props;
---

<div class="container mx-auto px-4">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-16 shadow-2xl rounded-xl">
        <div class="p-6">
            <div class="flex items-center mb-4">
                <h2 class="text-2xl font-bold">{title}</h2>
            </div>
            <p class="text-gray-600 mb-4">
                {description}
            </p>

            {
                elements.length > 0 && (
                    <ul class="space-y-2 mb-6">
                        {elements.map((element) => (
                            <li class="flex items-start">
                                <span>{element.text}</span>
                            </li>
                        ))}
                    </ul>
                )
            }
            <a class="" href="contact">
                <button class="px-4 py-2 bg-blue-600 hover:bg-blue-700 rounded text-semibold text-white">
                Solicitar presupuesto
                </button>
            </a>
        </div>

        <div class="bg-gray-100 rounded-r-lg overflow-hidden md:relative">
            <img
                src={imageUrl}
                alt="Control de cucarachas"
                class="w-full h-full object-cover md:absolute inset-0"
            />
        </div>
    </div>
</div>
